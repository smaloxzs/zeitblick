# Persönliche Tracking-Daten NICHT hochladen (bleiben lokal & privat)
data/

# Laufzeit-/temporäre Dateien
zeitblick.pid
focus_state.json
*.pyc
__pycache__/

# PyInstaller-Build-Ausgabe (die fertige .exe wird als GitHub-Release-Datei
# verteilt, nicht ins Repo committet — sonst blaeht das die Historie stark auf)
build/
dist/
*.spec
